# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AuthResponse {
  access_token: String!
}

type Document {
  id: Int!
  name: String!
  status: String!
  user: [User!]!
  userId: Float!
}

type Mutation {
  createDocument(data: String!): Boolean!
  createUser(input: UserDTO!): User!
  deleteFileInFolder(fileName: String!, id: Float!): Boolean!
  login(email: String!, password: String!): AuthResponse!
  uploadFile(content: String!, fileName: String!): String!
}

type Query {
  getUserById: User
  getUsers: [User!]!
  hello: String!
  listFilesInFolder: [Document!]!
}

"""User role (admin or user)"""
enum Role {
  ADMIN
  USER
}

type User {
  documents: [Document!]
  email: String!
  id: Int!
  name: String
  role: Role!
}

input UserDTO {
  email: String!
  name: String
  password: String!
  role: Role! = USER
}